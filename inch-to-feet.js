// inch to feet convert by function
function inchToFeet(inch) {
    var feet = inch / 12;
    return feet;
}
var amarRoom = inchToFeet(131);
console.log(amarRoom);

var torRoom = inchToFeet(333);
console.log(torRoom);

//There are three  variable keywords 1. var 2. const 3. let
// 1. var is reasignable মানে variable এর মান এই keyword এ Change kora jabe. but it is not professionaly will be used.
//2. const is not reasignalbel. এই keyword এর under এ কখনো  variable এর মান Changable na.
// 3. let is reasignable and it is also recommended to use in professionally.